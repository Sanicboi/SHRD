<UserControl
    x:Class="SHRD.TheoryCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SHRD"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="LightBlue" Padding="16 16 16 16" VerticalAlignment="Top" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="{x:Bind ImageUrl}" Width="200" Height="200" Margin="0 16 0 0"></Image>
        <TextBlock Grid.Row="1" TextAlignment="Center" Text="{x:Bind SubjectName}" Margin="0 0 0 0"></TextBlock>
    </Grid>
</UserControl>
